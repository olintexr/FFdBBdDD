# Introducción a SQL — Agregación y GROUP BY (04.4)

> Script de SQL Server con explicaciones y ejemplos. Los comentarios originales se conservan como texto y los bloques SQL se mantienen en formato de código.

## SECCIÓN 0. Cabecera del script: base `DemoSQL` para análisis de agrupaciones

**Propósito:**
- Presentar el entorno de trabajo para el capítulo de agregación y agrupación.
- Definir el dataset principal de la tabla `Pais`.
- Alinear la cabecera con el contenido del documento, que se centra en `GROUP BY`, `HAVING`, conteos y `CASE`.

**Contenido del archivo:**
- Creación de la base de datos `DemoSQL`.
- Definición de la tabla `Pais` con columnas de población, capital, idioma y continente.
- Carga de un conjunto reproducible de países para practicar grupos y resumenes.
- Ejercicios de agregación, conteo y clasificación por categorías.

**Notas de uso:**
- Este capítulo usa un dataset balanceado y reutilizable para análisis por continente e idioma.
- El bloque inicial prepara el entorno mínimo para ejecutar las consultas del archivo.
- La cabecera se centra en el contexto de agregación y análisis del material.

### Bloque inicial
```sql
-- Este bloque crea el entorno base para el capítulo de agrupación.
CREATE DATABASE DemoSQL;
GO

USE DemoSQL;
GO
```

## SECCIÓN 1. CREACIÓN DE LA BASE DE DATOS Y LA TABLA `Pais`

**Objetivos:**
- Crear una base de datos reproducible para practicar `GROUP BY`.
- Definir la estructura de la tabla `Pais`.
- Preparar el escenario para ejercicios de agregación y agrupamiento.

**Conceptos:**
- `CREATE DATABASE`: crea la base de datos de trabajo.
- `USE`: selecciona la base de datos activa.
- `CREATE TABLE`: define la estructura de una tabla.
- `IDENTITY PRIMARY KEY`: genera automáticamente la clave primaria.

**Errores frecuentes:**
- Olvidar seleccionar la base de datos con `USE`.
- Definir la tabla sin clave primaria.
- No respetar el tipo de dato para `Poblacion`.

### SQL de creación
```sql
CREATE DATABASE DemoSQL;
GO

USE DemoSQL;
GO

CREATE TABLE Pais (
    PaisID       INT IDENTITY PRIMARY KEY,
    Nombre       VARCHAR(100),
    Capital      VARCHAR(100),
    Poblacion    DECIMAL(10,1),
    Continente   VARCHAR(50),
    Idioma       VARCHAR(50)
);
GO
```

### Resumen de la sección
- Cómo crear una base de ejemplo sencilla y reutilizable.
- Cómo definir una tabla con una clave primaria automática.
- Cómo preparar el esquema necesario para prácticas de SQL.

**Conceptos que deben dominarse:**
- Sintaxis básica de `CREATE DATABASE` y `CREATE TABLE`.
- Uso de `IDENTITY` para claves de tipo entero.
- Importancia de mantener una estructura coherente para el análisis.

## SECCIÓN 2. CARGA DE DATOS CON DISTRIBUCIÓN DESIGUAL POR CONTINENTE (POBLACIÓN EN MILLONES)

**Objetivos:**
- Cargar datos realistas para practicar agrupaciones.
- Guardar la población en millones para simplificar la lectura y el análisis.
- Generar un conjunto de países con cantidades distintas por continente.
- Preparar consultas de conteo, suma y clasificación con un dataset más realista.

**Conceptos:**
- `INSERT INTO`: agrega filas a una tabla.
- `Poblacion` se guarda en millones, no en personas enteras.
- Un dataset con cantidades variadas por categoría permite observar diferencias en el resultado de `GROUP BY`.
- La consistencia de los datos es clave para los `GROUP BY`.

**Errores frecuentes:**
- Insertar valores absolutos en lugar de valores expresados en millones.
- Mezclar mayúsculas y minúsculas en forma no uniforme.
- Olvidar completar datos para todos los continentes.

### SQL de carga

_Los acentos se omitieron para evitar problemas de inserción en algunas bases de datos._

```sql
INSERT INTO Pais (Nombre, Capital, Poblacion, Continente, Idioma) VALUES
-- America
('Mexico', 'Ciudad de Mexico', 126.0, 'America', 'Espanol'),
('Estados Unidos', 'Washington D. C.', 331.0, 'America', 'Ingles'),
('Canada', 'Ottawa', 38.0, 'America', 'Ingles y frances'),
('Brasil', 'Brasilia', 212.0, 'America', 'Portugues'),
('Argentina', 'Buenos Aires', 45.0, 'America', 'Espanol'),
('Colombia', 'Bogota', 51.0, 'America', 'Espanol'),
('Chile', 'Santiago', 19.0, 'America', 'Espanol'),
('Peru', 'Lima', 33.0, 'America', 'Espanol'),
('Venezuela', 'Caracas', 28.0, 'America', 'Espanol'),
('Costa Rica', 'San Jose', 5.1, 'America', 'Espanol'),
('Guatemala', 'Ciudad de Guatemala', 18.0, 'America', 'Espanol'),
('Ecuador', 'Quito', 18.0, 'America', 'Espanol'),
('Uruguay', 'Montevideo', 3.4, 'America', 'Espanol'),
('Honduras', 'Tegucigalpa', 10.0, 'America', 'Espanol'),

-- Europa
('Espana', 'Madrid', 47.0, 'Europa', 'Espanol'),
('Francia', 'Paris', 67.0, 'Europa', 'Frances'),
('Alemania', 'Berlin', 83.0, 'Europa', 'Aleman'),
('Italia', 'Roma', 60.0, 'Europa', 'Italiano'),
('Reino Unido', 'Londres', 67.0, 'Europa', 'Ingles'),
('Portugal', 'Lisboa', 10.0, 'Europa', 'Portugues'),
('Paises Bajos', 'Amsterdam', 17.0, 'Europa', 'Neerlandes'),
('Suecia', 'Estocolmo', 10.0, 'Europa', 'Sueco'),
('Noruega', 'Oslo', 5.4, 'Europa', 'Noruego'),
('Suiza', 'Berna', 8.6, 'Europa', 'Aleman y frances'),
('Polonia', 'Varsovia', 37.0, 'Europa', 'Polaco'),
('Irlanda', 'Dublin', 5.3, 'Europa', 'Irlandes'),
('Rumania', 'Bucarest', 19.0, 'Europa', 'Rumano'),

-- Asia
('China', 'Beijing', 1400.0, 'Asia', 'Mandarin'),
('India', 'Nueva Delhi', 1380.0, 'Asia', 'Hindi'),
('Japon', 'Tokio', 125.0, 'Asia', 'Japones'),
('Corea del Sur', 'Seul', 52.0, 'Asia', 'Coreano'),
('Indonesia', 'Yakarta', 270.0, 'Asia', 'Indonesio'),
('Arabia Saudita', 'Riad', 34.0, 'Asia', 'Arabe'),
('Israel', 'Jerusalen', 9.0, 'Asia', 'Hebreo'),
('Turquia', 'Ankara', 83.0, 'Asia', 'Turco'),
('Tailandia', 'Bangkok', 70.0, 'Asia', 'Tailandes'),
('Vietnam', 'Hanoi', 97.0, 'Asia', 'Vietnamita'),
('Filipinas', 'Manila', 117.0, 'Asia', 'Filipino'),
('Nepal', 'Katmandu', 30.0, 'Asia', 'Nepali'),
('Bangladesh', 'Dacca', 172.0, 'Asia', 'Bengali'),
('Pakistan', 'Islamabad', 240.0, 'Asia', 'Urdu'),

-- Africa
('Nigeria', 'Abuya', 206.0, 'Africa', 'Ingles'),
('Egipto', 'El Cairo', 100.0, 'Africa', 'Arabe'),
('Sudafrica', 'Pretoria', 59.0, 'Africa', 'Ingles'),
('Etiopia', 'Adis Abeba', 114.0, 'Africa', 'Amharico'),
('Kenia', 'Nairobi', 53.0, 'Africa', 'Ingles y suajili'),
('Ghana', 'Acra', 31.0, 'Africa', 'Ingles'),
('Marruecos', 'Rabat', 36.0, 'Africa', 'Arabe'),
('Argelia', 'Argel', 43.0, 'Africa', 'Arabe'),
('Tanzania', 'Dodoma', 59.0, 'Africa', 'Suajili'),
('Senegal', 'Dakar', 16.0, 'Africa', 'Frances'),
('Uganda', 'Kampala', 48.0, 'Africa', 'Ingles'),
('Mozambique', 'Maputo', 33.0, 'Africa', 'Portugues'),
('Angola', 'Luanda', 36.0, 'Africa', 'Portugues'),

-- Oceania
('Australia', 'Canberra', 25.0, 'Oceania', 'Ingles'),
('Nueva Zelanda', 'Wellington', 5.0, 'Oceania', 'Ingles'),
('Fiyi', 'Suva', 0.9, 'Oceania', 'Ingles y fijiano'),
('Samoa', 'Apia', 0.2, 'Oceania', 'Samoano'),
('Tonga', 'Nukualofa', 0.1, 'Oceania', 'Tongano'),
('Papua Nueva Guinea', 'Port Moresby', 9.0, 'Oceania', 'Ingles y tok pisin'),
('Islas Salomon', 'Honiara', 0.7, 'Oceania', 'Ingles'),
('Vanuatu', 'Port Vila', 0.3, 'Oceania', 'Bislama'),
('Micronesia', 'Palikir', 0.1, 'Oceania', 'Ingles'),
('Kiribati', 'Tarawa Sur', 0.1, 'Oceania', 'Ingles'),
('Palau', 'Ngerulmud', 0.02, 'Oceania', 'Ingles y palauano');
GO
```

### Resumen de la sección
- Cómo poblar una tabla con ejemplos categóricos y reproducibles.
- Cómo generar un dataset útil para análisis por continente, idioma y capital.
- Cómo usar los datos para practicar agrupación y resumen con cantidades expresadas en millones.

**Conceptos que deben dominarse:**
- `INSERT INTO` con múltiples filas.
- Organización de datos por categorías.
- Reconocimiento del valor práctico de un dataset balanceado.

## SECCIÓN 3. CONCEPTOS FUNDAMENTALES DE `GROUP BY`

**Objetivos:**
- Comprender el flujo lógico de una consulta con `GROUP BY`.
- Reconocer la diferencia entre `WHERE`, `GROUP BY` y `HAVING`.
- Entender las reglas fundamentales para escribir agregaciones correctas.

**Conceptos:**
- `FROM`: define la fuente de datos.
- `WHERE`: filtra filas antes del agrupamiento.
- `GROUP BY`: agrupa filas por valor de una o varias columnas.
- `HAVING`: filtra grupos ya formados.
- `SELECT`: muestra el resultado final.
- `ORDER BY`: ordena la salida.

**Diferencia clave entre `WHERE` y `HAVING`:**
- `WHERE` se ejecuta antes de `GROUP BY` y elimina filas que no interesan.
- `HAVING` se ejecuta después de `GROUP BY` y elimina grupos completos.
- Por eso, si una condición puede evaluarse sobre la fila original, conviene ponerla en `WHERE`.
- Esto es más eficiente porque se reduce el número de filas antes de agrupar, y por tanto se hace menos trabajo en memoria y CPU.
- `HAVING` solo debe usarse cuando la condición depende de una función agregada, como `SUM`, `COUNT`, `AVG`, `MAX` o `MIN`.

**Errores frecuentes:**
- Incluir columnas en `SELECT` sin agregarlas ni agruparlas.
- Intentar usar `HAVING` para filtrar filas antes de agrupar.
- No distinguir entre una condición sobre filas y una condición sobre grupos.

### SQL de flujo lógico
```sql
-- Flujo lógico recomendado en consultas con agregación.
SELECT Continente, SUM(Poblacion) AS PoblacionTotal
FROM Pais
WHERE Poblacion >= 5
GROUP BY Continente
HAVING SUM(Poblacion) >= 100
ORDER BY Continente;
GO
```

### Resumen de la sección
- El orden correcto de evaluación de una consulta con agregación.
- Las reglas esenciales para usar `GROUP BY` sin errores.
- La diferencia entre `WHERE` y `HAVING`.
- Por qué es más eficiente filtrar en `WHERE` cuando la condición pertenece a filas individuales.

**Conceptos que deben dominarse:**
- El flujo lógico de evaluación.
- Uso de columnas agregadas y columnas de agrupación.
- Importancia de `HAVING` en consultas resumen.
- Ventaja de aplicar filtros tempranos para mejorar rendimiento.

## SECCIÓN 4. CONSULTAS DE AGREGACIÓN

**Objetivos:**
- Calcular poblaciones totales por continente e idioma.
- Identificar el valor de `SUM` como operación de resumen.
- Reconocer la utilidad de las funciones agregadas en reportes.

**Conceptos:**
- `SUM`: calcula la suma acumulada.
- `GROUP BY`: agrupa los registros antes de agregar.
- El resultado final cambia según la columna elegida para agrupar.

**Errores frecuentes:**
- Olvidar incluir la columna de agrupación en `GROUP BY`.
- Usar `SUM` sin tener claridad del nivel de detalle requerido.
- Confundir la agrupación por `Idioma` y por `Continente`.

### SQL de agregación
```sql
-- Población total por continente.
SELECT Continente, SUM(Poblacion) AS PoblacionTotal
FROM Pais
GROUP BY Continente;
GO

-- Población total por idioma.
SELECT Idioma, SUM(Poblacion) AS PoblacionTotal
FROM Pais
GROUP BY Idioma;
GO
```

### Resumen de la sección
- Cómo resumir datos con una sola función agregada.
- Cómo el resultado cambia dependiendo de la clave de agrupación.
- Cómo construir reportes simples con `GROUP BY`.

**Conceptos que deben dominarse:**
- Uso de `SUM` sobre columnas numéricas.
- Vínculo entre `GROUP BY` y la salida esperada.
- Lectura de resultados de resumen.

## SECCIÓN 5. CONTEOS

**Objetivos:**
- Aplicar `COUNT` para contar filas en grupos.
- Usar `COUNT DISTINCT` para contar categorías únicas.
- Explorar agrupaciones con expresiones derivadas.

**Conceptos:**
- `COUNT(*)`: cuenta filas completas.
- `COUNT(DISTINCT ...)`: cuenta valores únicos en una columna.
- `LEFT(...)`: extrae la inicial de un texto.

**Errores frecuentes:**
- Pensar que `COUNT` siempre cuenta valores distintos.
- No usar `DISTINCT` cuando se necesita contar categorías únicas.
- Olvidar que `LEFT(Capital,1)` debe aparecer también en `GROUP BY`.

### SQL de conteos
```sql
-- Conteo de países por continente.
SELECT Continente, COUNT(*) AS TotalPaises
FROM Pais
GROUP BY Continente;
GO

-- Conteo de países por idioma.
SELECT Idioma, COUNT(*) AS TotalPaises
FROM Pais
GROUP BY Idioma;
GO

-- Conteo de capitales por inicial.
SELECT LEFT(Capital,1) AS Inicial, COUNT(*) AS TotalCapitales
FROM Pais
WHERE Capital LIKE 'A%'
GROUP BY LEFT(Capital,1);
GO
```

### Resumen de la sección
- Cómo contar registros dentro de cada grupo.
- Cómo contar valores distintos cuando hace falta.
- Cómo usar expresiones simples para agrupar por patrones.

**Conceptos que deben dominarse:**
- Diferencia entre `COUNT(*)` y `COUNT(DISTINCT ...)`.
- Uso de `LEFT` dentro de agregaciones.
- Aplicación práctica de `WHERE` previo al `GROUP BY`.

## SECCIÓN 6. AGRUPAMIENTOS ADICIONALES E INTRODUCCIÓN A `CASE`

**Objetivos:**
- Clasificar datos mediante `CASE` dentro de un `GROUP BY`.
- Calcular promedios, máximos y mínimos por grupo.
- Explorar idiomas distintos por continente y filtros posteriores con `HAVING`.
- Entender una introducción sencilla a `CASE` usando una condición por rangos.

**Conceptos:**
- `CASE`: permite crear rangos o categorías.
- `AVG`: calcula el promedio por grupo.
- `MAX` y `MIN`: obtienen los valores extremos.
- `COUNT(DISTINCT ...)`: cuenta categorías distintas.

**Introducción sencilla a `CASE`:**
- `CASE` evalúa una condición y devuelve un valor según la primera condición que se cumpla.
- Es muy útil para agrupar filas en categorías como `Alto`, `Medio` o `Bajo`.
- En este ejemplo, la población ya está en millones, por lo que los límites se leen de forma más simple.

**Ejemplo base:**
```sql
SELECT 
    Nombre,
    Poblacion,
    CASE 
        WHEN Poblacion >= 100 THEN 'Alto'
        WHEN Poblacion >= 50 THEN 'Medio'
        ELSE 'Bajo'
    END AS descripcion
FROM Pais;
GO
```

**Errores frecuentes:**
- Repetir la expresión `CASE` en `SELECT` y `GROUP BY` sin tener un criterio claro.
- Usar `AVG` sin controlar el nivel de detalle de los datos.
- Confundir el resultado de `HAVING` con el de `WHERE`.

### SQL de agrupaciones adicionales
```sql
-- Rangos de población con CASE usando población en millones.
SELECT 
    CASE 
        WHEN Poblacion < 5 THEN 'Pequeno'
        WHEN Poblacion BETWEEN 5 AND 20 THEN 'Mediano'
        ELSE 'Grande'
    END AS RangoPoblacion,
    COUNT(*) AS TotalPaises
FROM Pais
GROUP BY 
    CASE 
        WHEN Poblacion < 5 THEN 'Pequeno'
        WHEN Poblacion BETWEEN 5 AND 20 THEN 'Mediano'
        ELSE 'Grande'
    END;
GO

-- Promedio de población por continente.
SELECT Continente, AVG(Poblacion) AS PromedioPoblacion
FROM Pais
GROUP BY Continente;
GO

-- Máximo y mínimo por idioma.
SELECT Idioma,
       MAX(Poblacion) AS MaxPoblacion,
       MIN(Poblacion) AS MinPoblacion
FROM Pais
GROUP BY Idioma;
GO

-- Idiomas distintos por continente.
SELECT Continente, COUNT(DISTINCT Idioma) AS IdiomasDistintos
FROM Pais
GROUP BY Continente;
GO

-- Capitales agrupadas por inicial.
SELECT LEFT(Capital,1) AS Inicial, COUNT(*) AS Total
FROM Pais
GROUP BY LEFT(Capital,1)
ORDER BY Inicial;
GO

-- Uso de HAVING.
SELECT Continente, COUNT(*) AS TotalPaises
FROM Pais
GROUP BY Continente
HAVING COUNT(*) > 5;
GO
```

### Resumen de la sección
- Cómo construir categorías con `CASE` para reportes más claros.
- Cómo resumir datos con `AVG`, `MAX` y `MIN`.
- Cómo `HAVING` refina la salida final luego del agrupamiento.
- Cómo usar `CASE` como una forma simple de etiquetar filas y grupos.

**Conceptos que deben dominarse:**
- Uso de `CASE` para crear rangos.
- Diferencia de resultado entre `GROUP BY` y `HAVING`.
- Aplicación práctica de `COUNT(DISTINCT ...)`.

## SECCIÓN 7. EJERCICIOS PROPUESTOS

**Objetivos:**
- Consolidar el uso de `GROUP BY` con ejercicios progresivos.
- Practicar consultas de resumen, conteo y clasificación.
- Reforzar la diferencia entre niveles básicos, intermedios y avanzados.

**Conceptos:**
- Los ejercicios sirven para aplicar lo aprendido sin depender de una sola consulta.
- Cada ejercicio busca reforzar una habilidad distinta del análisis de datos.
- La práctica repetida mejora la lectura de resultados y la construcción de reportes.

**Errores frecuentes:**
- Tratar de resolver varios ejercicios a la vez sin validar cada paso.
- Olvidar que algunas respuestas requieren `HAVING` o `ORDER BY`.
- Hacer consultas demasiado complejas antes de dominar la base.

### Nivel básico
1. Obtener la población total por idioma.
2. Listar los continentes ordenados por cantidad de países.
3. Mostrar los idiomas cuyo promedio de población sea mayor a 50 millones.

### Nivel intermedio
4. Clasificar los países por rangos de población usando `CASE`.
5. Obtener las iniciales de capitales y su conteo.
6. Mostrar los continentes que tengan más de 3 idiomas distintos.

### Nivel avanzado
7. Calcular el país más poblado por continente.
8. Mostrar los idiomas cuyo mínimo de población sea menor a 1 millón.
9. Obtener la suma de población de los países cuya capital comience por vocal.
10. Crear una consulta que combine `GROUP BY`, `HAVING` y `ORDER BY`.

### Resumen de la sección
- Cómo convertir la teoría en práctica con ejercicios progresivos.
- Qué tipo de razonamiento exige cada nivel.
- La importancia de la repetición para dominar agrupaciones.

**Conceptos que deben dominarse:**
- Construir consultas de resumen paso a paso.
- Integrar `GROUP BY`, `HAVING` y `ORDER BY` en una misma solución.
- Resolver problemas con una estrategia clara y comprobable.

## SECCIÓN 8. CONCLUSIONES

**Objetivos:**
- Cerrar el capítulo con una visión general de lo aprendido.
- Reconocer la utilidad de `GROUP BY` en análisis de datos.
- Destacar la importancia de la práctica aplicada.

**Conceptos:**
- `GROUP BY` permite resumir información con estructura y claridad.
- El conjunto de datos de `Pais` facilita el aprendizaje de agregaciones y filtros.
- Los ejercicios propuestos permiten avanzar de lo básico a lo avanzado.

**Errores frecuentes:**
- Tratar `GROUP BY` como una consulta de lectura simple sin considerar el resumen.
- Ignorar la diferencia entre las columnas que se muestran y las columnas que se agregan.
- No practicar con varios ejemplos para internalizar el patrón.

### Resumen final
- Este capítulo entrega una base sólida para comprender y practicar `GROUP BY` en SQL Server.
- La tabla `Pais` facilita el estudio de agregaciones, rangos, conteos y filtros sobre grupos.
- Los ejercicios propuestos permiten consolidar la teoría con práctica realista y progresiva.

**Conceptos que deben dominarse:**
- Agrupar y resumir datos con criterio.
- Aplicar funciones agregadas de forma correcta.
- Combinar `GROUP BY`, `HAVING` y `ORDER BY` en consultas funcionales.
